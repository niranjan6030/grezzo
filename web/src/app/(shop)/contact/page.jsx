import ContactView from "@/components/ContactView";

export const metadata = {
  title: "Contact",
  description: "Reach Grezzo — sizing, shipping, returns, privacy and cookies.",
};

export default function ContactPage() {
  return <ContactView />;
}
